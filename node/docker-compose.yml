version: '3.8'

services:
  sl1-node:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./ledger.json:/app/ledger.json
    environment:
      - NODE_ENV=production
    restart: always
