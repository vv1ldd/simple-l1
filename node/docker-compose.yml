version: '3.8'

services:
  sl1-node:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./ledger_db.json:/app/ledger_db.json
    environment:
      - NODE_ENV=production
    restart: always
